Place the QuickVina2 executable here as `qvina2.1` before building the image.

The `docker/build-image.sh` helper will copy it from `CONDITAR_QVINA_BIN`,
`--qvina-bin`, or the OSC default path when available.
