apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: conditar-gui
  labels:
    app: conditar-gui
spec:
  to:
    kind: Service
    name: conditar-gui
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
